// Shared by targeting, simulation and UI; keep all intent sources on one cap.
export const COMBAT = Object.freeze({ stepMs:700, damageScale:.7, intentOnAttack:12, intentOnHit:10, intentCap:100 });
export const RULES_VERSION = 9;
