# 美术资源与游戏规则分离

当前项目是 Web 原型。新表现层接入本地图片、逐帧动画、OBJ 模型及 GLB 模型，游戏启动不需要 Unity、Godot、Blender 或 npm install。

## 已接入的本地试玩包

- 来源一：`C:\Users\tao50\Documents\New project\sango_infinity-main`。
- 来源二：`C:\Users\tao50\Documents\三国策略`。
- 当前索引：713 名武将头像；8 类兵种待机、移动、攻击动画映射；8 个模型条目；7 张战斗特效图、1 张通用暴击插画；1 张首页背景及 4 张原项目全国地图底图。共同引用的文件去重后为 744 个外部文件，导入时没有缺失文件。
- OBJ：走舸、冲车、运输车；通过 Unity Prefab 和材质 GUID 找到网格及配套贴图。当前“兵器”以冲车作为统一试玩外观，不从外观推断射程或攻击方式。
- GLB：城门完好/受损、树林、山地、木桥。按现有规则坐标布置，城门受损时更换模型。
- 动画：原资源表中的兵种图集逐帧播放；Unity `transport_move.anim` 的根位移曲线保留关键帧与 Hermite 切线，转换为资源清单中的表现数据。模型移动、轻微攻击摆动由表现时钟驱动。
- 枪/戟、弓/弩等部分兵种共用当前素材，保留兵种文字徽标以便区分。头像同名变体取最小编号；游戏数据中真正重名的武将及自建武将不自动匹配。

这不是完整 Unity 场景、Animator、骨骼、Shader 或粒子系统的移植。FBX、Unity 专用材质及其他未映射资源仍需单独转换；本次已把可用的模型和动画实际用于战斗，而非只做资源浏览器。

## 启动与切换

双击 `start-game.cmd`，或在项目目录执行 `npm start`。如果之前启动过旧服务，关闭该服务再启动。当前机器已生成 `art.local.json` 与 `.local/art/manifest.json`，默认启用本地资源。

首页和战斗右上角齿轮菜单提供“美术：本地资源 / 默认图形”。切换只保存浏览器显示偏好，不修改战斗存档。缺图保留文字头像，缺模型使用兵种图集，图集也不可用时回到原有图标；WebGL 不可用时自动使用二维表现。

资源目录移动后重新执行：

```powershell
npm run art:import -- "C:\你的目录\sango_infinity-main" "C:\你的目录\三国策略"
```

随后重启服务。不带参数的 `npm run art:import` 复用本机配置。也可用环境变量 `SANGO_ART=off` 禁止服务提供本地资源。

## 分层与替换接口

### 普通模式全国地图

普通模式的“天下大势”默认使用重新绘制的二维山河舆图，详见文末。支持全国/中原视野、四季换色、拖动和缩放。原项目 `BaseMap0–3.png` 的导入能力仍保留在本地资源清单中（秋 0、春 1、夏 2、冬 3），当前地图不再使用这些底图。

`art-strategic-map.mjs` 统一投影城池、军团行军、道路、粮道和遭遇标记；`strategic-map-art.mjs` 集中定义绘制内容与显示坐标。规则坐标、道路长度、行军天数和存档保持不变，地图不受本地资源开关影响。

`npm run art:verify-map` 验证普通模式桌面/手机画面、城池与军团点击、全国/中原视图、缩放、季节切换、存档不变及文件缺失回退。此次 27 项美术与战略相关测试通过，截图在 `outputs/local-art/national-*.png`。

```text
规则模块：武将 ID、兵种 ID、坐标、伤亡、战斗事件
                       ↓ 只读快照
art-battle.mjs：动画时钟、插值、逐帧图片和事件特效
art-models.mjs：OBJ / GLB 网格、材质、摆放及释放
                       ↓
art-assets.mjs：资源清单、ID 映射、加载与回退
                       ↓
local-art-server.mjs：仅向本机提供清单登记过的文件
```

规则层不导入这些模块，不读取模型大小、动画时长、图片路径或加载完成时间。新动画不推进模拟步，不回调伤害，不消耗规则随机数。已有程序战法演出与结算流程继续使用原机制。已有规则数据中的 `icon` / `visual` 提示字段仍保留以兼容当前显示，它们不绑定文件路径或资源包。

暂停、弹窗、后台状态冻结表现时钟；倍速改变播放速度；减少动态效果偏好显示静态帧。单步及读档不补播上一批事件。切换战场会释放 WebGL、几何、材质、纹理、观察器和动画循环。清单按稳定武将 ID / 兵种 ID / 地形类型映射，换素材只改资源包，不改规则和存档。

替换包的公开清单使用 `version: 1`，包含 `portraits`、`troops`、`models`、`effects`、`backgrounds` 和 `animations`。图集条目为 `{url, columns, rows, frames, duration, loop}`；模型条目为 `{format: "obj"|"glb", url, texture?}`。当前本地服务使用 SHA-256 前缀生成不含本机路径的资源 URL；私有文件表与来源哈希只留在 `.local/art/manifest.json`。美术包不能覆盖属性、技能、碰撞、占格或地形规则。

## 本地资源隔离与发布

原始资源留在外部目录。本机配置、清单、工具依赖及包含素材的验收截图被 Git 忽略；Service Worker 不缓存 `/local-art/`。本地美术需要本机服务在线，离线模式回退为默认图形。

`npm run build:release` 在 `dist/release-时间戳` 生成新的代码发行目录，只复制运行时代码、数据与 Three.js；不复制 `.local`、本机配置、截图和外部素材。干净目录启动时使用默认图形。不要将整个工作目录直接作为发行包。

这个打包步骤负责隔离本地美术，不替代对已有武将数据、列传等内容的来源审核，也不把这些试玩素材标记为可商用。

## 验证

- `npm test`：360 项通过（含新资源 URL 校验、曲线采样、文件白名单、规则层依赖隔离）。
- `npm run check`：语法检查通过。
- `npm run art:verify`：需要 Playwright；可通过 `PLAYWRIGHT_MODULE` 指定安装路径。覆盖四场历史战役、OBJ/GLB 实际加载、暂停帧冻结、美术切换不改存档、头像加载、手机画面、文件 404 回退及离线缓存隔离。
- 本机截图保存在 `outputs/local-art/`，验证结果为 `ui-result.json`；该目录不提交、不打包。

## 二维山河舆图（当前默认大地图）

`strategic-map-art.mjs` 提供独立绘制的 SVG 山脉、河流、海岸、林地、农田、城楼和港口；参考原地图的表现方向，未嵌入原地图贴图。`art-strategic-map.mjs` 负责城市、军团、粮道与战斗标记交互。四季换色、全国/中原视野、拖动和缩放只影响 UI。

显示坐标集中在 `MAP_CITIES`，行军插值仍使用规则层的 `roadLength`；显示坐标与绘图不进入存档。此图是适配当前九城玩法的示意图，不是精确地理复原。外部美术关闭、丢失或发布包没有本地资源时，二维地图仍可用。原四季贴图导入接口保留，默认地图不使用它。

`npm run art:verify-map` 验证城池/军团操作、视野与季节切换不改存档、移动端无横向溢出以及缺失外部素材时的地图显示。


## 战斗动画更新（2026-09-18）

普通部队采用六人步兵／三人骑兵队列，增加阵营旗帜、脚下投影、四方向图集选择、平滑移动、扬尘、攻击前摇、突刺和受击后仰。普通攻击增加弧线箭矢、刀光、命中火花和延后出现的伤害数字；清晰模式保留简化后的动作反馈。

战法演出分为人物切入、战法名称展开、退场和战场效果四段。同次施放的多个命中仍合并一次演出，真实暴击使用 `CriticalImage/default.png`。该资源是通用插画，不代表每名武将的专属立绘；非暴击使用已有武将头像，缺失时显示姓名标识。

暂停、隐藏页面及战法演出期间的表现时钟分别处理，动画不会写回战斗状态。规则、暴击率、结算和存档结构不变。现有 OBJ／GLB 展示继续保留；当前是 2.5D 图集与静态模型混合表现，未新增三维骨骼动画。

验证：`npm run check`、`npm test`（369 项）及 `scripts/verify-art-ui.mjs`、`scripts/verify-battle-animation.mjs`。后者从真实官渡战斗获取普通攻击、战法和暴击，检查暂停冻结、窄屏和减少动态效果；截图位于 `outputs/local-art/animation/`。本地资源索引已经重新生成；此前启动的服务需重新启动才能读到新增暴击图。


### 战法演出分级

100 战意的大战法（八百破阵、虎卫折冲、十胜奇谋、赤壁业火、火烧连营、无双乱舞、苍天雷殛）和真实连携保留人物插画演出。其他战法用局部光环、短光点、战法名与伤害／治疗数字表示，不暂停模拟，小战法暴击也不升级为切屏。大战法名单由当前战法配置的战意门槛读取。

连携标记与原战法全部命中合并到一次演出，保留真实结算与记录，避免战法和连携各打断一次。清晰／完整模式均遵守此分级。


### 军略与持续状态（2026-09-19）

军略拥有独立的军令旗帜、横扫光幕与受影响部队的六角阵纹：进攻使用金色、援护使用青绿、扰敌使用紫色。敌我军令分别标明阵营，不进入战法人物演出队列，也不暂停战斗。暂停下令会立即显示静止的军令提示，继续战斗后播放并淡出；刷新存档不重播历史军令。

部队的持续状态从真实个人状态和军略状态读取。眩晕显示头顶三颗环绕星星，混乱显示旋涡，封技显示锁印；灼烧显示火焰，护盾／减伤显示蓝色罩体，治疗显示上升十字，增益显示光环和上升箭头，减益显示虚线环与向下箭头。部队侧面保留高对比状态字标，优先控制与持续伤害；过多状态用数量标记，完整名称、来源、剩余时间仍在部队详情里。图标跟随移动，到期、净化和离场后随真实状态清除。减少动态效果设置改用静止标识。

验证：389 项测试通过，`scripts/verify-battle-signals.mjs` 使用真实军略操作及真实战斗存档验证双方军令、眩晕、混乱、灼烧、护盾、暂停、窄屏和默认图形。截图位于 `outputs/local-art/signals/`。
