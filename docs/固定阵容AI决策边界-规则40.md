# 固定阵容 AI 决策边界（规则40确认，规则41更新）

更新日期：2026-09-19。按用户最新确认，双方阵容与学习记录固定后，AI 的可优化决策只保留：**出阵顺序、初始出阵位置、军略选择与施放时机**。

## 决策范围

| 项目 | 后续 AI 优化范围 | 当前实现 |
| --- | --- | --- |
| 出阵顺序 | 决定哪些部队先出阵，以及预备队补位的先后 | 规则41已实现敌方按战况择序：初始与每次补位时，从已到达活预备队中逐一评分，兼顾兵力、前排、支援、克制与攻城需求；玩家仍按既定队列 |
| 初始位置 | 在合法布阵格内安排前后排、侧翼、支援距离和地形利用 | `planEnemyArmy` 已根据兵种、兵力、地形、现有战法和可见对面站位摆放在场部队 |
| 军略 | 从实际掌握的军略中选择施放内容与时机，必要时保留蓄力等待 | `chooseEnemyCommand` 已按当前战况评分，再由双方共用的 `issueCommand` 检查资格、蓄力和冷却 |

出阵顺序与初始位置是两件事。摆棋时为了先放前排而临时排序，不等于改变真实出阵队列。后续择序也必须遵守六队上场、援军实际到达、合法地形与占位规则；不能让尚未到达的援军提前参战。

## 战法是固定输入

双方使用同一套[战法随机学习规则](战法随机学习-规则40.md)：携带当前兵种全部已学普通战法，并额外携带已学专属。普通战法最多三种，专属额外占位，合计最多四种；未学满可以少于四种，也可以暂时没有战法。

- AI 不按敌阵、职责或地形选择、替换或重抽战法。
- 学到暂时没有用途的战法仍保留；施放时没有合法目标就跳过，不转换成另一项技能。
- AI 保持已有合法施放顺序，不将重排战法作为第四种优化手段。玩家现有的战前顺序调整入口不由本条移除。
- 换兵种、重复布阵、存读档不重新抽取学习结果。
- 当前规划器在携带记录不合法时，仅恢复学习记录规定的完整列表；这是恢复合法输入，不是依据敌阵重新选技能。

`availableTactics` 仍可用于描述某兵种及武将的理论战法池，但真正的携带资格由 `learnedTacticIds` 和 `validLoadout` 判断。不能沿用规则39“从六种基础战法加专属中自由挑三种”的说法。

## 战中共同引擎继续执行

限制 AI 的优化范围不等于取消自动战斗。双方部队仍由共用引擎自动走位、普攻、寻找战法目标、检查门槛与冷却并施放。已有战法顺序决定逐项检查顺序，一个槽位无法施放时继续检查下一项。

因此战斗链条是：

```text
固定阵容、兵力、等级、学习记录、战法顺序及场景条件
→ 场景设置地形、城门及援军到达时刻
→ 敌方逐队选择首发；我方使用既定队列
→ 初始部队入场并安排合法站位
→ 共用引擎处理状态、走位、普攻与战法；出现空位时敌方重评合法预备队
→ AI 根据可见战况选择军略与时机
→ 共用引擎判断胜负
```

现有战前意图也作为场景或已配置输入处理，不增加为本次确认的第四个 AI 优化变量。歼灭、固守、攻城仍会依原有规则影响普通目标评分；本次范围确认没有修改它们的执行规则。

## 后续验证要求

比较三类决策的收益时，应保持双方武将、兵种、等级、兵力、任命、学习记录、战法顺序、姿态、意图、地形、援军时刻和随机种子一致，只改变待比较的出阵顺序、初始站位或军略策略。批量评估使用独立验证种子，不能靠重新抽到有利战法提高胜率。

双方始终共用战法效果、属性计算、行动规则和军略门槛。初始学习结果和关卡资源可以不同，但 AI 不获得未学战法、额外槽位、免费军略或额外行动。

规则39的[完整 AI 说明](固定阵容战斗AI决策全流程-规则39.md)保留为历史快照，其中角色模板选装、自由三槽与对应验证结论已经过时，不作为当前配置规则或平衡结论。规则41的具体评分、接入时序、行为回归和配对对照见[出阵择序与验证](deployment-order-v41/出阵择序与验证.md)。择序只改变哪些预备队先进入空位，不重排本方逐步行动数组，不主动轮换在场部队。

源码入口：[battle-ai.mjs](../battle-ai.mjs)、[tactic-learning.mjs](../tactic-learning.mjs)、[tactics.mjs](../tactics.mjs)、[engine.mjs](../engine.mjs)。
